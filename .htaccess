Options +FollowSymLinks
RewriteEngine On
RewriteBase /

# Remove .html extension
RewriteCond %{THE_REQUEST} \s/+(.+)\.html[\s?]
RewriteRule ^ %1 [R=302,L,NE]

# Internally rewrite request to .html
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^(.+)$ $1.html [L]
